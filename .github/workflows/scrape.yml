name: SAFLII Crime Scraper

on:
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Tor
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y tor
          # GH Actions has no systemd ? start tor directly as a process
          sudo tor --RunAsDaemon 1 --Log "notice file /tmp/tor.log"
          echo "Waiting for Tor to bootstrap..."
          for i in $(seq 1 24); do
            sleep 5
            if grep -q "Bootstrapped 100%" /tmp/tor.log 2>/dev/null; then
              echo "Tor bootstrapped after $((i*5))s"
              break
            fi
            echo "  ...${i}/${24} waiting"
          done
          cat /tmp/tor.log | tail -5

      - name: Verify Tor
        run: |
          python3 -c "
          import requests, warnings
          warnings.filterwarnings('ignore')
          try:
              r = requests.get(
                  'https://check.torproject.org/api/ip',
                  proxies={'http':'socks5://127.0.0.1:9050','https':'socks5://127.0.0.1:9050'},
                  timeout=15, verify=False
              )
              d = r.json()
              print('Tor active:', d.get('IsTor'), '| Exit IP:', d.get('IP'))
          except Exception as e:
              print('Tor check failed (non-fatal):', e)
          "

      - name: Install Python deps
        run: pip install playwright pdfplumber "requests[socks]" PySocks beautifulsoup4

      - name: Install Playwright browsers
        run: |
          playwright install chromium
          playwright install-deps chromium

      - name: Run scraper
        run: python scraper.py

      - name: Upload cases
        uses: actions/upload-artifact@v4
        with:
          name: saflii-cases-${{ github.run_number }}
          path: cases/
          retention-days: 90
